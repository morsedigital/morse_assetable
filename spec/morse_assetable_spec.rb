require 'spec_helper'

describe MorseAssetable do
  it 'has a version number' do
    expect(MorseAssetable::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
